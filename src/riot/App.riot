<app id={'app'}>
    <section is="layout" {...state} />
  <script>
    import Layout from './components/layout.riot';
    export default {
      components: {
        Layout,
      },
      onBeforeMount(props, state) {
        console.log('before mount')
        console.log('state', state)
        console.log('props', props)
        Object.assign(state, props);
        console.log('******************', this)
        const self = this;
        
      }
    }
  </script>
</app>
